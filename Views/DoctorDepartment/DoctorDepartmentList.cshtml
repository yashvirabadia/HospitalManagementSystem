@model DataTable
@using System.Data

<section class="section">
    <div class="row">
        <div class="col-lg-12">

            <div class="card">
                <div class="card-body">
                    <h5 class="card-title mb-4 fw-semibold text-primary fs-3">DoctorDepartment List</h5>


                    @if (TempData["ErrorMessage"] != null)
                    {
                        <div class="alert alert-danger alert-dismissible fade show" role="alert">
                            @TempData["ErrorMessage"]
                            <button type="button" class="btn-close" data-bs-dismiss="alert" aria-label="Close"></button>
                        </div>
                    }
                    @if (TempData["Message"] != null)
                    {
                        <div class="alert alert-success alert-dismissible fade show" role="alert">
                            @TempData["Message"]
                            <button type="button" class="btn-close" data-bs-dismiss="alert" aria-label="Close"></button>
                        </div>
                    }


                    <!-- Responsive Table Wrapper -->
                    <div class="table-responsive">
                        <table class="table">
                            <thead>
                                <tr>
                                    @* <th scope="col">DocDepID</th> *@
                                    <th scope="col">Action</th>
                                    <th scope="col">DepartmentName</th>
                                    <th scope="col">DoctorName</th>
                                    <th scope="col">Qualification</th>
                                    <th scope="col">UserName</th>
                                    <th scope="col">UserEmail</th>
                                    <th scope="col">MobileNo</th>
                                    <th scope="col">Created</th>
                                    <th scope="col">Modified</th>
                                </tr>
                            </thead>
                            <tbody>
                                @foreach (DataRow row in Model.Rows)
                                {
                                    <tr>
                                        @* <th>@row["DoctorDepartmentID"]</th> *@
                                        <td>
                                            <div class="d-flex gap-2">
                                                <a class="btn btn-outline-success btn-xs" asp-controller="DoctorDepartment" asp-action="DoctorDepartmentFormFill" asp-route-ID="@row["DoctorDepartmentID"]">
                                                    <i class="bi bi-pencil-fill"></i>
                                                </a>

                                                <a asp-controller="DoctorDepartment" asp-action="DoctorDepartmentDelete" asp-route-DoctorDepartmentID="@row["DoctorDepartmentID"]" class="btn btn-outline-danger btn-xs">
                                                    <i class="bi bi-x"></i>
                                                </a>
                                            </div>
                                        </td>
                                        <td>@row["DepartmentName"]</td>
                                        <td>@row["DoctorName"]</td>
                                        <td>@row["Qualification"]</td>
                                        <td>@row["UserName"]</td>
                                        <td>@row["Email"]</td>
                                        <td>@row["MobileNo"]</td>
                                        <td>@Convert.ToDateTime(row["Created"]).ToString("dd-MM-yyyy")</td>
                                        <td>@Convert.ToDateTime(row["Modified"]).ToString("dd-MM-yyyy")</td>
                                        
                                    </tr>
                                }
                            </tbody>
                        </table>
                    </div>
                    <!-- End Responsive Table Wrapper -->

                </div>
            </div>

        </div>
    </div>
</section>
