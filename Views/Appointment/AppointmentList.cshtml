@model DataTable
@using System.Data

<section class="section">
    <div class="row">
        <div class="col-lg-12">

            <div class="card">
                <div class="card-body">
                    <h5 class="card-title mb-4 fw-semibold text-primary fs-3">Appointment List</h5>

                    
                    @if (TempData["ErrorMessage"] != null)
                    {
                        <div class="alert alert-danger alert-dismissible fade show" role="alert">
                            @TempData["ErrorMessage"]
                            <button type="button" class="btn-close" data-bs-dismiss="alert" aria-label="Close"></button>
                        </div>
                    }
                    @if (TempData["Message"] != null)
                    {
                        <div class="alert alert-success alert-dismissible fade show" role="alert">
                            @TempData["Message"]
                            <button type="button" class="btn-close" data-bs-dismiss="alert" aria-label="Close"></button>
                        </div>
                    }



                    <div class="table-responsive">
                        <table class="table">
                            <thead>
                                <tr>
                                    
                                    @* <th scope="col">AppID</th> *@
                                    <th scope="col">Action</th>
                                    <th scope="col">DocName</th>
                                    <th scope="col">Specialization</th>
                                    <th scope="col">PatientName</th>
                                    <th scope="col">Gender</th>
                                    <th scope="col">AppDate</th>
                                    <th scope="col">Status</th>
                                    <th scope="col">Description</th>
                                    <th scope="col">SpecialRemarks</th>
                                    <th scope="col">Amount</th>
                                    <th scope="col">Created</th>
                                    <th scope="col">Modified</th>
                                </tr>
                            </thead>
                            <tbody>
                                @foreach (DataRow row in Model.Rows)
                                {
                                    <tr>

                                        @* <th>@row["AppointmentID"]</th> *@
                                        <td>
                                            <div class="d-flex gap-2">
                                                <a class="btn btn-outline-success btn-xs" asp-controller="Appointment" asp-action="AppointmentFormFill" asp-route-ID="@row["AppointmentID"]">
                                                    <i class="bi bi-pencil-fill"></i>
                                                </a>

                                                <a asp-controller="Appointment" asp-action="AppointmentDelete" asp-route-AppointmentID="@row["AppointmentID"]" class="btn btn-outline-danger btn-xs">
                                                    <i class="bi bi-x"></i>
                                                </a>
                                            </div>

                                        </td>
                                       
                                        <td>@row["DoctorName"]</td>
                                        <td>@row["Specialization"]</td>
                                        <td>@row["PatientName"]</td>
                                        <td>@row["Gender"]</td>
                                        <td>@row["AppointmentDate"]</td>
                                        <td>@row["AppointmentStatus"]</td>
                                        <td>@row["Description"]</td>
                                        <td>@row["SpecialRemarks"]</td>
                                        <td>@row["TotalConsultedAmount"]</td>
                                        <td>@Convert.ToDateTime(row["Created"]).ToString("dd-MM-yyyy")</td>
                                        <td>@Convert.ToDateTime(row["Modified"]).ToString("dd-MM-yyyy")</td>
                                        @* <td>@row["Created"]</td>
                                        <td>@row["Modified"]</td> *@
                                        
                                    </tr>
                                }
                            </tbody>
                        </table>
                    </div>


                </div>
            </div>

        </div>
    </div>
</section>
